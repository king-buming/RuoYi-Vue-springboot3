# 开发日志

## 2026-06-02

### 手机端 H5 完成
- 新增 `/app/*` 后端接口（Auth/Checkin/Worker），不走后台鉴权
- HMAC Token 认证方案（AppTokenUtil），7 天有效期
- 打卡规则：禁止重复签到/签退、禁止未签到先签退
- 归档/禁用人员统一拦截（isWorkerActive 含 status 检查）
- 打卡拍照改为先上传 /common/upload 再拿 URL 入库
- `mobile.html` 纯 H5 单文件：登录/打卡/记录/我的/身份证上传/资质上传/人脸上传
- 底部三 Tab 导航，密码密文输入，定时器防泄漏

### RuoYi-App uni-app 端
- 6 个工人页面完成：login / checkin / records / profile / certs / face
- 统一权限守卫（permission.js）：工人端走 appToken，系统端走原有 token
- 打卡照片改为先 upload 再提交（checkin.vue 接入 utils/upload.js）
- ⚠ uni-app 页面需要 HBuilderX 编译，WSL 环境无法直接运行

### 接入同伴 dev-homework-worker 分支
- 同伴将 worker 模块从 com.ruoyi.worker.* 合并入 com.ruoyi.system.*
- 同伴实现了作业管理模块：hw_plan（作业计划）、hw_attendance（进场/离场打卡+Quartz 超时检测）、hw_worker（9 角色字符串）
- 两套人员/打卡体系并存，后续需要统一

### 待办
- [ ] 与同伴沟通：合并 tb_worker 和 hw_worker 的人员体系
- [ ] 作业计划 hw_plan 与我们的人员模块做关联
- [ ] HBuilderX 编译 uni-app 页面（Windows 端）
- [ ] 完整联调验收：手机端登录→上传→签到→后台查看→审核
- [ ] 公众号菜单、微信 OAuth、消息推送

## 2026-06-01

### Phase 1 完成
- tb_worker 人员档案：CRUD + 角色分配 + 身份证推断性别 + 单位类型
- tb_worker_role 角色规则：CRUD + 联动字段 + 资质校验
- tb_worker_cert 资质证件：CRUD + 图片上传 + 人员下拉
- tb_worker_face 人脸信息：CRUD + 照片上传 + 特征值预留
- tb_worker_audit 审核记录：CRUD + Service 层回写闭环 (@Transactional)
- tb_worker_checkin 打卡记录：CRUD + 时间范围区间查询
- tb_worker_role_rel 多对多关联
- 审核闭环：新增/修改审核记录自动回写主表状态
- 角色规则弱约束：缺资质警告+退回待审核
- 人员归档：软删除 + isWorkerActive 前置校验
- 身份证唯一校验、手机/身份证格式校验
- keep-alive activated 自动刷新
- 交付文档：Phase1_业务流程说明.md

## 2026-05-31

### 环境搭建
- 克隆仓库，配置本地开发环境（JDK17/Maven/MySQL/Redis/Node）
- 建库 ry-vue，本地账号 ruoyi/Ruoyi@2026
- 配置 git skip-worktree 保护本地凭据
- sql/updates/ 约定目录建立（团队数据库同步机制）
- 后端 8080 / 前端 80 启动验证通过
